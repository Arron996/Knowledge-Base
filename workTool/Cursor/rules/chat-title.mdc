---
description: 会话标题 rename_chat。首轮及主题漂移时设简体中文标题；Plan 模式不豁免，首轮结束前自检。
alwaysApply: true
---

# 会话标题（rename_chat）

## 必须执行

对话标题须为**简体中文**，准确反映当前讨论主题（建议 ≤20 字）。

| 时机 | 动作 |
|------|------|
| 新对话首轮 | 理解意图并完成首轮实质性回复后，调用 MCP `rename_chat` |
| 主题明显偏离 | 稳定 2–3 轮后再更新；同主题最多改 1–2 次；标题已基本准确则不动 |

避免泛化词（如「帮助」「问题」）或纯英文标题。

## Plan 模式（不豁免）

处于 **Plan 模式**、调研代码、产出计划或 `CreatePlan` **均不**推迟或省略 `rename_chat`：

- 标题设置与调研、写计划**并行**，在首轮用户可见回复发出**前**完成（可与工具调用同轮）。
- 计划初稿阶段可用暂定标题；用户确认或主题收窄后，按上表规则**最多再更新 1–2 次**。

Plan 模式禁止写业务代码，**不**禁止改会话标题。

## 首轮结束前自检

发出首轮回复前快速确认：

1. 是否已理解用户主诉求（而非仅复述问题）？
2. 是否已调用 `cursor-app-control` → `rename_chat`？
3. 标题是否为简体中文且概括当前任务？

若 1 已满足而 2 未做 → **先 `rename_chat` 再结束回合**。仅当 1 不满足（纯问候、主题完全不可识别）时可跳过 2。

## 示例

| 用户诉求 | 标题 |
|----------|------|
| Ticket Log 退票对齐四项 + 异常配置默认码 | 退票对齐优化与异常配置 |
| 制定 F2B 合 base 计划 | F2B 合入测试分支计划 |
| 优化 rule 漏设标题 | 会话标题 rule 优化 |
