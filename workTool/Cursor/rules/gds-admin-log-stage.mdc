---
description: Admin MCP 查日志前必须先定 stage/actions。用户排障、查 ticket log、查订单日志、msgTrace、售后变更、退改出票等凡涉及 user-gds-order-admin-mcp 的 query-ticket-logs / query-order-logs / query-logs 时使用。
alwaysApply: false
---

# Admin MCP 日志查询：先 stage/action，再 MCP

凡调用 **user-gds-order-admin-mcp** 查操作日志（`query-ticket-logs` / `query-order-logs` / `query-logs`）：

1. **Read** `.cursor/skills/gds-rail-triage/stage-playbook.md`（速查表 + 代码锚点）
2. 场景复杂时 **Read** `.cursor/skills/gds-rail-triage/SKILL.md`（完整 SOP）

## 必须遵守

- **先代码、后 MCP**：从 `TicketLogStageRegistry.java` / `QmqConstant` / `@QmqConsumer` 解析 `stage` 或 `actions`
- **优先** `stage`（内置阶段）或 `actions`（精确 action/topic）；**禁止起手** `filterKey` 广撒网
- `filterKey` 仅用于缩小子单号、refundId、orderItemId
- Mongo `action` ≠ BAT CLOG `title`；WARN/堆栈用 `bat-clog` + `messageId`
- 第一轮并行：`pre-query-order` + `query-ticket-logs`（已定参）

## 快速入口

| 需求 | 参数 |
|------|------|
| 创单/出票/退改 | `stage=book_order` / `pay_order` / `apply_refund` / …（见 playbook 表） |
| 售后变更 | `actions=train.global.rail.gds.order.ticketing.monitor.alteration,CompleteAlterationConsumerService` |
| 推订单是否发出 | `actions=train.global.rail.gds.order.after.sale.change` |

完整映射见 **stage-playbook.md**。
